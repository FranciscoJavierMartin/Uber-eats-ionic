import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uber-eats-clone-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
